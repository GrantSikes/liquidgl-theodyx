import puppeteer from 'puppeteer-core'; import lighthouse from 'lighthouse';
const CHROME='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome';
const url=process.argv[2]; const only=process.argv[3]; const variants=[{n:'normal',block:[]},{n:'nofont',block:['*GoogleSansFlex*']},{n:'nogsap',block:['*/gsap/*']}];
for (const v of variants.filter(v=>!only||v.n===only)) for (let i=1;i<=2;i++){ const browser=await puppeteer.launch({executablePath:CHROME,headless:'new',args:['--no-first-run']}); const port=new URL(browser.wsEndpoint()).port; const r=await lighthouse(url,{port,output:'json',logLevel:'error',onlyCategories:['performance'],blockedUrlPatterns:v.block}); const A=r.lhr.audits; (await import('node:fs')).writeFileSync('../build/lh-exp-'+new URL(url).host.split('.')[0]+'-'+v.n+'-'+i+'.json', JSON.stringify(r.lhr)); const m=A['metrics'].details.items[0]; console.log(v.n,i,'perf',Math.round(r.lhr.categories.performance.score*100),'FCP',Math.round(m.firstContentfulPaint),'LCP',Math.round(m.largestContentfulPaint),'obsFCP',Math.round(m.observedFirstContentfulPaint),'SI',Math.round(m.speedIndex)); await browser.close(); }
